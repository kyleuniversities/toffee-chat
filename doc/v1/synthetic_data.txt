// General
general.user.bios = [Array]
general.user.profileImages = [Array]
general.user.thumbnailImages = [Array]

// Messaging Template
* Introductions
* What brings you 
* What do you think of X?
* It's Getting Late
* When do you want to talk next?
* Set Up
* Good night & take care!

// Users
user.names = [Array]
user.usernames = user.name + (randomNumber)
user.emails = user.usernames + "@gamil.com"
user.passwords = bcrypt(user.usernames)
user.roles = User
user.bios = general.user.bios(user.names)
user.profileImages = general.user.profileImages(user.names)
user.thumbnailImages = general.user.thumbnailImages(user.names)

//  User.Message Template
user.messagetemplate.names = user.names
user.messagetemplate.introductions = user.names >> [Array]
user.messagetemplate.bring = user.names >>[Array]
user.messagetemplate.think = user.names >>[Array]

// Rooms
rooms = user ** user >> 8

// Connections
connections = user ** user >> 12

// Messages
messages = createMessage(user1.messagetemplate, user2.messagetemplate) 

