mutation($userId: String!, $body: String!) {
    createPost(userId: $userId, body: $body) {
        id,
        body,
        numberOfLikes,
        numberOfComments,
        user {
            name
        }
    }
}